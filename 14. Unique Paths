## 14. Unique Paths

```python
m = int(input("Rows: "))
n = int(input("Columns: "))

dp = [[1] * n for _ in range(m)]

for i in range(1, m):
    for j in range(1, n):
        dp[i][j] = dp[i-1][j] + dp[i][j-1]

print("Unique Paths =", dp[m-1][n-1])
```
